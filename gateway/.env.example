# Server
PORT=3001
NODE_ENV=development

# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/api_gateway?schema=public"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Internal API Base URL (where requests are forwarded)
INTERNAL_API_BASE_URL=https://example.com/api

# JWT Secret (for future auth)
JWT_SECRET=your-secret-key-change-in-production
