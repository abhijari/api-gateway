apiVersion: v1
kind: Secret
metadata:
  name: gateway-secret
  namespace: api-gateway
type: Opaque
stringData:
  # Example only. Do NOT commit real credentials.
  # Create this secret at deploy time (kubectl/CI/Secret Manager/SSM).
  DATABASE_URL: "postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
