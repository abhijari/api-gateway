apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: api-gateway
data:
  NODE_ENV: production
  # Example only. Do NOT commit real credentials.
  # Prefer setting REDIS_URL via Secret instead.
  REDIS_URL: "redis://HOST:6379"