env           = "prod"
instance_type = "t2.micro"

gateway_config = {
  NODE_ENV   = "production"
  RATE_LIMIT = "500"
  REDIS_HOST = "redis"
  REDIS_PORT = 6379
}

gateway_secrets = {
  DATABASE_URL = "<REQUIRED>"
}

dashboard_config = {
  DATABASE_URL = "<REQUIRED>"
}

dashboard_secrets = {}